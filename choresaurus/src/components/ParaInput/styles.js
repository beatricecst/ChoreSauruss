import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'flex-start'
    },
    text: {
        fontSize: 15,
        marginBottom: 15,
    },
    textInput: {
        marginBottom: 100,
    }
})